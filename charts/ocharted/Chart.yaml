---
apiVersion: v2
name: ocharted
description: A stateless OCI registry proxy that serves classic Helm repositories as OCI artifacts — point Flux OCIRepositories or `helm install oci://` at any HTTP Helm repo with no onboarding
type: application
kubeVersion: ">=1.25.0-0"
# release-please stamps both from the release tag (the release workflow also
# passes --version/--app-version at package time); stamping the committed copy
# keeps it from going stale.
version: 0.1.1 # x-release-please-version
appVersion: "0.1.1" # x-release-please-version
home: https://github.com/home-operations/ocharted
sources:
  - https://github.com/home-operations/ocharted
maintainers:
  - name: home-operations
    email: contact@home-operations.com
keywords:
  - ocharted
  - helm
  - oci
  - registry
  - proxy
  - flux
  - kubernetes
annotations:
  category: Infrastructure
